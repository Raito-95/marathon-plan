[project]
name = "marathon-plan"
version = "0.1.0"
description = "由設定產生全馬或半馬的備賽課表，並輸出成文字、Markdown、JSON 或 LINE 推播"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
